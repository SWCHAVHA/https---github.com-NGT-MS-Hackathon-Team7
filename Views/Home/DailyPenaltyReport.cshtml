@model List<ReportModel>

<h1>Daily Penalty Report</h1>

@if (Model != null && Model.Count > 0)
{
    @foreach (var report in Model)
    {
        @Html.Partial("ReportModel", report)
    }
}
else
{
    <p>No data available for the daily penalty report.</p>
}
